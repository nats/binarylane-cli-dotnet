#nullable enable
// <auto-generated/>
using System.CommandLine;
using System.Text;
using System.Text.Json;
using BinaryLane.Cli.Commands;
using BinaryLane.Cli.Infrastructure.Json;
using BinaryLane.Cli.Infrastructure.Output;

namespace BinaryLane.Cli.Generated.Commands;

public static class ImageUpdateCommand
{
    public static Command Create()
    {
        var command = new Command("update", "Update an existing image");

        var image_idArg = new Argument<string>("image_id") { Description = "The ID of the image to update." };
        command.Arguments.Add(image_idArg);

        var nameOpt = new Option<string?>("--name") { Description = "Optional: a new display name for this image. Do not provide to leave the display name unchanged, submit an empty string to clear the display name." };
        command.Options.Add(nameOpt);

        var lockedOpt = new Option<bool?>("--locked") { Description = "Optional: you may choose to lock an individual backup in which case we will not update that backup until you unlock it. Do not provide to leave the locked status unchanged. You may not lock or unlock a temporary backup or a backup that is attached to a server." };
        command.Options.Add(lockedOpt);

        command.SetAction(async (parseResult, ct) =>
        {
            var cmdCtx = ContextBinder.Bind(parseResult);
            var image_id = parseResult.GetValue(image_idArg)!;
            var name = parseResult.GetValue(nameOpt);
            var locked = parseResult.GetValue(lockedOpt);

            var body = new Dictionary<string, object?>();
            if (name != null) body["name"] = name;
            if (locked != null) body["locked"] = locked;

            return await ApiCommandBase.ExecuteApiCallAsync(
                cmdCtx,
                async client => await client.PutAsync($"v2/images/{image_id}", new StringContent(JsonSerializer.Serialize(body, CliJsonContext.Default.DictionaryStringObject), Encoding.UTF8, "application/json"), ct),
                null);
        });

        return command;
    }
}
