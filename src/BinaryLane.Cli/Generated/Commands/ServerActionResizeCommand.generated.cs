#nullable enable
// <auto-generated/>
using System.CommandLine;
using System.Text;
using System.Text.Json;
using BinaryLane.Cli.Commands;
using BinaryLane.Cli.Infrastructure.Json;
using BinaryLane.Cli.Infrastructure.Output;

namespace BinaryLane.Cli.Generated.Commands;

public static class ServerActionResizeCommand
{
    public static Command Create()
    {
        var command = new Command("resize", "Update the size and related options for a server");

        var server_idArg = new Argument<string>("server_id") { Description = "The ID of the server on which the action should be performed." };
        command.Arguments.Add(server_idArg);

        var sizeOpt = new Option<string?>("--size") { Description = "The slug of the selected size. Do not provide to keep the current size." };
        command.Options.Add(sizeOpt);

        var optionsOpt = new Option<string?>("--options") { Description = "If this is null and the server has no selected size options the defaults for the size will be used. If this is null and the server has currently selected size options those will be preserved. If this is provided any option fields that are not included will be removed from the existing server." };
        command.Options.Add(optionsOpt);

        var change_imageOpt = new Option<string?>("--change-image") { Description = "This may be left null to keep the current base image for the server. If this is provided the server disks will be destroyed and the server will be rebuilt from the selected image." };
        command.Options.Add(change_imageOpt);

        var change_licensesOpt = new Option<string?>("--change-licenses") { Description = "This may be left null to keep the current licenses for the server. If this is provided any licenses that are not included will be removed. Any licences that are invalid will be removed regardless, see the documentation for `servers/{server_id}/software` for more information." };
        command.Options.Add(change_licensesOpt);

        var pre_action_backupOpt = new Option<string?>("--pre-action-backup") { Description = "Specify this to create a backup before any actions are taken, or leave null to skip." };
        command.Options.Add(pre_action_backupOpt);

        command.SetAction(async (parseResult, ct) =>
        {
            var cmdCtx = ContextBinder.Bind(parseResult);
            var server_id = parseResult.GetValue(server_idArg)!;
            var size = parseResult.GetValue(sizeOpt);
            var options = parseResult.GetValue(optionsOpt);
            var change_image = parseResult.GetValue(change_imageOpt);
            var change_licenses = parseResult.GetValue(change_licensesOpt);
            var pre_action_backup = parseResult.GetValue(pre_action_backupOpt);

            var body = new Dictionary<string, object?>();
            body["type"] = "resize";
            if (size != null) body["size"] = size;
            if (options != null) body["options"] = options;
            if (change_image != null) body["change_image"] = change_image;
            if (change_licenses != null) body["change_licenses"] = change_licenses;
            if (pre_action_backup != null) body["pre_action_backup"] = pre_action_backup;

            return await ApiCommandBase.ExecuteApiCallAsync(
                cmdCtx,
                async client => await client.PostAsync($"v2/servers/{server_id}/actions", new StringContent(JsonSerializer.Serialize(body, CliJsonContext.Default.DictionaryStringObject), Encoding.UTF8, "application/json"), ct),
                null);
        });

        return command;
    }
}
