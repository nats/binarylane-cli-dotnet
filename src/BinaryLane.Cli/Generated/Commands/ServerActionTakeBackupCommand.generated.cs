#nullable enable
// <auto-generated/>
using System.CommandLine;
using System.Text;
using System.Text.Json;
using BinaryLane.Cli.Commands;
using BinaryLane.Cli.Infrastructure.Json;
using BinaryLane.Cli.Infrastructure.Output;

namespace BinaryLane.Cli.Generated.Commands;

public static class ServerActionTakeBackupCommand
{
    public static Command Create()
    {
        var command = new Command("take-backup", "Take a backup of a server");

        var server_idArg = new Argument<string>("server_id") { Description = "The ID of the server on which the action should be performed." };
        command.Arguments.Add(server_idArg);

        var backup_typeOpt = new Option<string?>("--backup-type") { Description = "If replacement_strategy is anything other than 'specified', this must be provided.  | Value | Description | | ----- | ----------- | | daily | A backup which is scheduled to be taken each day. | | weekly | A backup which is scheduled to be taken each week. | | monthly | A backup which is scheduled to be taken each month. | | temporary | A backup which is created on demand and only retained for a maximum of seven days. |  " };
        command.Options.Add(backup_typeOpt);

        var replacement_strategyOpt = new Option<string>("--replacement-strategy") { Description = "The strategy for selecting which backup to replace (if any).  | Value | Description | | ----- | ----------- | | none | Do not replace any existing backup: use a free slot of the provided backup type. If there are no free slots an error will occur. | | specified | Replace the specific backup id provided. | | oldest | Use any free slots of the provided backup type, and if there are no free slots replace the oldest unlocked and un-attached backup of the provided backup type. | | newest | Use any free slots of the provided backup type, and if there are no free slots replace the newest unlocked and un-attached backup of the provided backup type. |  " };
        replacement_strategyOpt.Required = true;
        command.Options.Add(replacement_strategyOpt);

        var backup_id_to_replaceOpt = new Option<int?>("--backup-id-to-replace") { Description = "If replacement_strategy is 'specified' this property must be set to an existing backup." };
        command.Options.Add(backup_id_to_replaceOpt);

        var labelOpt = new Option<string?>("--label") { Description = "An optional label to identify the backup." };
        command.Options.Add(labelOpt);

        command.SetAction(async (parseResult, ct) =>
        {
            var cmdCtx = ContextBinder.Bind(parseResult);
            var server_id = parseResult.GetValue(server_idArg)!;
            var backup_type = parseResult.GetValue(backup_typeOpt);
            var replacement_strategy = parseResult.GetValue(replacement_strategyOpt);
            var backup_id_to_replace = parseResult.GetValue(backup_id_to_replaceOpt);
            var label = parseResult.GetValue(labelOpt);

            var body = new Dictionary<string, object?>();
            body["type"] = "take_backup";
            if (backup_type != null) body["backup_type"] = backup_type;
            if (replacement_strategy != null) body["replacement_strategy"] = replacement_strategy;
            if (backup_id_to_replace != null) body["backup_id_to_replace"] = backup_id_to_replace;
            if (label != null) body["label"] = label;

            return await ApiCommandBase.ExecuteApiCallAsync(
                cmdCtx,
                async client => await client.PostAsync($"v2/servers/{server_id}/actions", new StringContent(JsonSerializer.Serialize(body, CliJsonContext.Default.DictionaryStringObject), Encoding.UTF8, "application/json"), ct),
                null);
        });

        return command;
    }
}
