#nullable enable
// <auto-generated/>
using System.CommandLine;
using System.Text;
using System.Text.Json;
using BinaryLane.Cli.Commands;
using BinaryLane.Cli.Infrastructure.Json;
using BinaryLane.Cli.Infrastructure.Output;

namespace BinaryLane.Cli.Generated.Commands;

public static class ServerCreateCommand
{
    public static Command Create()
    {
        var command = new Command("create", "Create a new server");

        var nameOpt = new Option<string?>("--name") { Description = "The hostname of your server, such as vps01.yourcompany.com. If not provided, the server will be created with a random name." };
        command.Options.Add(nameOpt);

        var backupsOpt = new Option<bool?>("--backups") { Description = "If true this will enable two daily backups for the server. Options.daily_backups will override this value if provided. Setting this to false has no effect." };
        command.Options.Add(backupsOpt);

        var ipv6Opt = new Option<bool?>("--ipv6") { Description = "If true this will enable IPv6 for this server." };
        command.Options.Add(ipv6Opt);

        var sizeOpt = new Option<string>("--size") { Description = "The slug of the selected size." };
        sizeOpt.Required = true;
        command.Options.Add(sizeOpt);

        var imageOpt = new Option<int>("--image") { Description = "The slug or id of the selected operating system." };
        imageOpt.Required = true;
        command.Options.Add(imageOpt);

        var regionOpt = new Option<string>("--region") { Description = "The slug of the selected region." };
        regionOpt.Required = true;
        command.Options.Add(regionOpt);

        var vpc_idOpt = new Option<int?>("--vpc-id") { Description = "Leave null to use default (public) network for the selected region." };
        command.Options.Add(vpc_idOpt);

        var vpc_ipv4_addressOpt = new Option<string?>("--vpc-ipv4-address") { Description = "If provided this will be the Ipv4 address for the server's private VPC network adapter. If this is null an unused Ipv4 address will be assigned. This field is only valid when VpcId is provided." };
        command.Options.Add(vpc_ipv4_addressOpt);

        var ssh_keysOpt = new Option<string[]>("--ssh-keys") { Description = "This may be either the SSH keys Ids or fingerprints. If this is null or not provided any SSH keys that have been marked as default will be deployed (if the operating system supports SSH keys). Submit an empty array to disable deployment of default keys.", AllowMultipleArgumentsPerToken = true };
        command.Options.Add(ssh_keysOpt);

        var optionsOpt = new Option<string?>("--options") { Description = "This may be left null to accept all of the defaults for the selected size." };
        command.Options.Add(optionsOpt);

        var licensesOpt = new Option<string[]>("--licenses") { Description = "The desired set of licenses.", AllowMultipleArgumentsPerToken = true };
        command.Options.Add(licensesOpt);

        var user_dataOpt = new Option<string?>("--user-data") { Description = "If provided this will be used to initialise the new server. This must be left null if the Image does not support UserData, see DistributionInfo.Features for more information." };
        command.Options.Add(user_dataOpt);

        var port_blockingOpt = new Option<bool?>("--port-blocking") { Description = "Port blocking of outgoing connections for email, SSH and Remote Desktop (TCP ports 22, 25, and 3389) is enabled by default for all new servers. If this is false port blocking will be disabled. Disabling port blocking is only available to reviewed accounts." };
        command.Options.Add(port_blockingOpt);

        var passwordOpt = new Option<string?>("--password") { Description = "If this is provided the default remote user account's password will be set to this value. If this is null a random password will be generated and emailed to the account email address." };
        command.Options.Add(passwordOpt);

        command.SetAction(async (parseResult, ct) =>
        {
            var cmdCtx = ContextBinder.Bind(parseResult);
            var name = parseResult.GetValue(nameOpt);
            var backups = parseResult.GetValue(backupsOpt);
            var ipv6 = parseResult.GetValue(ipv6Opt);
            var size = parseResult.GetValue(sizeOpt);
            var image = parseResult.GetValue(imageOpt);
            var region = parseResult.GetValue(regionOpt);
            var vpc_id = parseResult.GetValue(vpc_idOpt);
            var vpc_ipv4_address = parseResult.GetValue(vpc_ipv4_addressOpt);
            var ssh_keys = parseResult.GetValue(ssh_keysOpt);
            var options = parseResult.GetValue(optionsOpt);
            var licenses = parseResult.GetValue(licensesOpt);
            var user_data = parseResult.GetValue(user_dataOpt);
            var port_blocking = parseResult.GetValue(port_blockingOpt);
            var password = parseResult.GetValue(passwordOpt);

            var body = new Dictionary<string, object?>();
            if (name != null) body["name"] = name;
            if (backups != null) body["backups"] = backups;
            if (ipv6 != null) body["ipv6"] = ipv6;
            if (size != null) body["size"] = size;
            body["image"] = image;
            if (region != null) body["region"] = region;
            if (vpc_id != null) body["vpc_id"] = vpc_id;
            if (vpc_ipv4_address != null) body["vpc_ipv4_address"] = vpc_ipv4_address;
            if (ssh_keys != null) body["ssh_keys"] = ssh_keys;
            if (options != null) body["options"] = options;
            if (licenses != null) body["licenses"] = licenses;
            if (user_data != null) body["user_data"] = user_data;
            if (port_blocking != null) body["port_blocking"] = port_blocking;
            if (password != null) body["password"] = password;

            return await ApiCommandBase.ExecuteApiCallAsync(
                cmdCtx,
                async client => await client.PostAsync($"v2/servers", new StringContent(JsonSerializer.Serialize(body, CliJsonContext.Default.DictionaryStringObject), Encoding.UTF8, "application/json"), ct),
                null);
        });

        return command;
    }
}
