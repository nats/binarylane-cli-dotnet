#nullable enable
// <auto-generated/>
using System.CommandLine;
using System.Text;
using System.Text.Json;
using BinaryLane.Cli.Commands;
using BinaryLane.Cli.Infrastructure.Json;
using BinaryLane.Cli.Infrastructure.Output;

namespace BinaryLane.Cli.Generated.Commands;

public static class VpcPatchCommand
{
    public static Command Create()
    {
        var command = new Command("patch", "Update an existing VPC");

        var vpc_idArg = new Argument<string>("vpc_id") { Description = "The target vpc id." };
        command.Arguments.Add(vpc_idArg);

        var nameOpt = new Option<string?>("--name") { Description = ">A name to help identify this VPC. Submit null to leave unaltered." };
        command.Options.Add(nameOpt);

        var route_entriesOpt = new Option<string[]>("--route-entries") { Description = "Submit null to leave unaltered, submit an empty list to clear all route entries. It is not possible to PATCH individual route entries, to alter a route entry submit the entire list of route entries you wish to save.", AllowMultipleArgumentsPerToken = true };
        command.Options.Add(route_entriesOpt);

        command.SetAction(async (parseResult, ct) =>
        {
            var cmdCtx = ContextBinder.Bind(parseResult);
            var vpc_id = parseResult.GetValue(vpc_idArg)!;
            var name = parseResult.GetValue(nameOpt);
            var route_entries = parseResult.GetValue(route_entriesOpt);

            var body = new Dictionary<string, object?>();
            if (name != null) body["name"] = name;
            if (route_entries != null) body["route_entries"] = route_entries;

            return await ApiCommandBase.ExecuteApiCallAsync(
                cmdCtx,
                async client => await client.PatchAsync($"v2/vpcs/{vpc_id}", new StringContent(JsonSerializer.Serialize(body, CliJsonContext.Default.DictionaryStringObject), Encoding.UTF8, "application/json"), ct),
                null);
        });

        return command;
    }
}
