#nullable enable
// <auto-generated/>
using System.CommandLine;
using System.Text;
using System.Text.Json;
using BinaryLane.Cli.Commands;
using BinaryLane.Cli.Infrastructure.Json;
using BinaryLane.Cli.Infrastructure.Output;

namespace BinaryLane.Cli.Generated.Commands;

public static class LoadBalancerUpdateCommand
{
    public static Command Create()
    {
        var command = new Command("update", "Update an existing load balancer");

        var load_balancer_idArg = new Argument<string>("load_balancer_id") { Description = "The ID of the load balancer to update." };
        command.Arguments.Add(load_balancer_idArg);

        var nameOpt = new Option<string>("--name") { Description = "The hostname of the load balancer." };
        nameOpt.Required = true;
        command.Options.Add(nameOpt);

        var forwarding_rulesOpt = new Option<string[]>("--forwarding-rules") { Description = "The rules that control which traffic the load balancer will forward to servers in the pool. Leave null to accept a default \"HTTP\" only forwarding rule.", AllowMultipleArgumentsPerToken = true };
        command.Options.Add(forwarding_rulesOpt);

        var health_checkOpt = new Option<string?>("--health-check") { Description = "The rules that determine which servers are considered 'healthy' and in the server pool for the load balancer. Leave this null to accept appropriate defaults based on the forwarding_rules." };
        command.Options.Add(health_checkOpt);

        var server_idsOpt = new Option<string[]>("--server-ids") { Description = "A list of server IDs to assign to this load balancer.", AllowMultipleArgumentsPerToken = true };
        command.Options.Add(server_idsOpt);

        command.SetAction(async (parseResult, ct) =>
        {
            var cmdCtx = ContextBinder.Bind(parseResult);
            var load_balancer_id = parseResult.GetValue(load_balancer_idArg)!;
            var name = parseResult.GetValue(nameOpt);
            var forwarding_rules = parseResult.GetValue(forwarding_rulesOpt);
            var health_check = parseResult.GetValue(health_checkOpt);
            var server_ids = parseResult.GetValue(server_idsOpt);

            var body = new Dictionary<string, object?>();
            if (name != null) body["name"] = name;
            if (forwarding_rules != null) body["forwarding_rules"] = forwarding_rules;
            if (health_check != null) body["health_check"] = health_check;
            if (server_ids != null) body["server_ids"] = server_ids;

            return await ApiCommandBase.ExecuteApiCallAsync(
                cmdCtx,
                async client => await client.PutAsync($"v2/load_balancers/{load_balancer_id}", new StringContent(JsonSerializer.Serialize(body, CliJsonContext.Default.DictionaryStringObject), Encoding.UTF8, "application/json"), ct),
                null);
        });

        return command;
    }
}
