#nullable enable
// <auto-generated/>
using System.CommandLine;
using System.Text;
using System.Text.Json;
using BinaryLane.Cli.Commands;
using BinaryLane.Cli.Infrastructure.Json;
using BinaryLane.Cli.Infrastructure.Output;

namespace BinaryLane.Cli.Generated.Commands;

public static class VpcCreateCommand
{
    public static Command Create()
    {
        var command = new Command("create", "Create a new VPC");

        var nameOpt = new Option<string>("--name") { Description = "A name to help identify this VPC." };
        nameOpt.Required = true;
        command.Options.Add(nameOpt);

        var route_entriesOpt = new Option<string[]>("--route-entries") { Description = "The route entries that control how network traffic is directed through the VPC environment.", AllowMultipleArgumentsPerToken = true };
        command.Options.Add(route_entriesOpt);

        var ip_rangeOpt = new Option<string?>("--ip-range") { Description = "A private address range that you select during creation, such as the default value of 10.240.0.0/16. Because the virtual network is dedicated to your use, you may use whatever IP address range you like." };
        command.Options.Add(ip_rangeOpt);

        command.SetAction(async (parseResult, ct) =>
        {
            var cmdCtx = ContextBinder.Bind(parseResult);
            var name = parseResult.GetValue(nameOpt);
            var route_entries = parseResult.GetValue(route_entriesOpt);
            var ip_range = parseResult.GetValue(ip_rangeOpt);

            var body = new Dictionary<string, object?>();
            if (name != null) body["name"] = name;
            if (route_entries != null) body["route_entries"] = route_entries;
            if (ip_range != null) body["ip_range"] = ip_range;

            return await ApiCommandBase.ExecuteApiCallAsync(
                cmdCtx,
                async client => await client.PostAsync($"v2/vpcs", new StringContent(JsonSerializer.Serialize(body, CliJsonContext.Default.DictionaryStringObject), Encoding.UTF8, "application/json"), ct),
                null);
        });

        return command;
    }
}
