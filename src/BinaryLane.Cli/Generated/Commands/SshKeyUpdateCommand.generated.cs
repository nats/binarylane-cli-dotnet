#nullable enable
// <auto-generated/>
using System.CommandLine;
using System.Text;
using System.Text.Json;
using BinaryLane.Cli.Commands;
using BinaryLane.Cli.Infrastructure.Json;
using BinaryLane.Cli.Infrastructure.Output;

namespace BinaryLane.Cli.Generated.Commands;

public static class SshKeyUpdateCommand
{
    public static Command Create()
    {
        var command = new Command("update", "Update an existing SSH key");

        var key_idArg = new Argument<string>("key_id") { Description = "The ID or fingerprint of the SSH Key to update." };
        command.Arguments.Add(key_idArg);

        var nameOpt = new Option<string>("--name") { Description = "A name to help you identify the key." };
        nameOpt.Required = true;
        command.Options.Add(nameOpt);

        var @defaultOpt = new Option<bool?>("--default") { Description = "Do not provide or leave null to leave the default status of the key unchanged. Optional: If true this will be added to all new server installations (if we support SSH Key injection for the server's operating system)." };
        command.Options.Add(@defaultOpt);

        command.SetAction(async (parseResult, ct) =>
        {
            var cmdCtx = ContextBinder.Bind(parseResult);
            var key_id = parseResult.GetValue(key_idArg)!;
            var name = parseResult.GetValue(nameOpt);
            var @default = parseResult.GetValue(@defaultOpt);

            var body = new Dictionary<string, object?>();
            if (name != null) body["name"] = name;
            if (@default != null) body["default"] = @default;

            return await ApiCommandBase.ExecuteApiCallAsync(
                cmdCtx,
                async client => await client.PutAsync($"v2/account/keys/{key_id}", new StringContent(JsonSerializer.Serialize(body, CliJsonContext.Default.DictionaryStringObject), Encoding.UTF8, "application/json"), ct),
                null);
        });

        return command;
    }
}
